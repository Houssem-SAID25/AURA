"""
onboarding/steps/__init__.py
=============================
Step modules for the AURA onboarding wizard.

Each step module exposes a ``run(data: dict) -> dict`` function that
accepts the accumulated profile data and returns new/updated fields.
"""
